apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: {{.Appname}}
  labels:
    app: {{.Appname}}
spec:
  replicas: 3
  selector:
    matchLabels:
      app: {{.Appname}}
  template:
    metadata:
      labels:
        app: {{.Appname}}
    spec:
      containers:
      - name: {{.Appname}}
        image: {{.Image}}
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: {{.Appname}}
spec:
  type: NodePort
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: {{.Appname}}
